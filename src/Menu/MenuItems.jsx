import React from "react";
import styled from "styled-components";

const MenuItems = () => {
  return <StyledMenuItems>MenuItems</StyledMenuItems>;
};

const StyledMenuItems = styled.div`
  color: var(--white);
`;

export default MenuItems;
